# Contracts

Shared contracts belong here when the frontend and backend start sharing generated types or API definitions.

Recommended future contents:

- OpenAPI source or generated spec
- shared TypeScript types derived from the backend contract
- validation schemas used by multiple apps

For now this directory is intentionally small so the monorepo has a clean place for cross-app code.
