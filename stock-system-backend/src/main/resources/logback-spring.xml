<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define JSON encoder for console output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</timestampPattern>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <thread>thread</thread>
                <logger>logger</logger>
                <level>level</level>
                <levelValue>level_value</levelValue>
                <stackTrace>stack_trace</stackTrace>
                <mdc>mdc</mdc>
            </fieldNames>
            <!-- Include MDC (Mapped Diagnostic Context) -->
            <includeMdc>true</includeMdc>
            <!-- Custom fields to identify service -->
            <customFields>{"service":"stock-system-backend","environment":"${spring.profiles.active:-default}"}</customFields>
        </encoder>
    </appender>

    <!-- Logger for our application packages -->
    <logger name="com.stock" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="com.external" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="org.springframework" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="org.hibernate" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>